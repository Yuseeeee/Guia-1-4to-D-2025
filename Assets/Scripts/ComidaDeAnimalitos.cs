using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ComidaDeAnimalitos : MonoBehaviour
{
    public string codigoAnimal "";
    int comidaPorDiaGato = 300;
    int comidaPorDiaPerro = 400;
    int comidaPorDiaPerroG = 700;
    public int diasEstancia;
    // Start is called before the first frame update
    void Start()
    {
        if (codigoAnimal == "G")
        {

        }
        else if (codigoAnimal == "PP")
            {

            }
        else if (codigoAnimal == "PG")
                {

                }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
